# Kalshi 15M metals desk — adversarial review (2026-09-10)

Five independent skeptic agents, each required to verify findings by running
code against the harvested data, not by reading. Ordered most severe first
within each surface. This is the record the verdict in `kalshi_backtest.md`
rests on; every CONFIRMED finding was reproduced.

## Fill realism

- **[critical]** (✓ verified) The 'next-candle-open confirms' fill filter is vacuous: the backtest is a zero-latency, always-filled model
  - quantfirm/kalshi/backtest.py:288-311 claims conservatism ('filled only if the NEXT candle's side-price OPEN is at/inside our limit'). Measured on the harvested candles: the next candle's side-price open EQUALS the prior candle's close 98.7% (gold), 98.4% (silver), 96.0% (copper) of all minute pairs — the 'open' is a carry-forward snapshot at T+~0s, i.e. the same quote the decision was made on. Rerunning the chosen config (theta=0.07, hl=30, tau 120-600s, price 0.35-0.92; reproduces TEST n=215 pnl=-57.01 exactly): only 3 of 218 fill attempts gap away (1.4%); ungated across all 1,452 intents, 99
  - _Fix:_ Stop describing next-open confirmation as conservative. Replace with a fill model that imposes real latency: harvest the public trades tape (GET /markets/trades) and require the limit level to still be printing at/inside for L seconds after T before granting a fill; report results as a latency curve
- **[critical]** (✓ verified) All backtest profit sits inside a sub-2-5-second race the live engine cannot win; at realistic latency the strategy loses on every split
  - Timed every trade of the chosen config against the prod trades tape (first trade strictly beyond the limit in the fill minute = generous deadline for the IOC, since quotes can be pulled before any print). TEST (215 trades): 78% of fill minutes have a beyond-limit print; deadline p25=1.4s, p50=4.1s, p90=33s; 27% within 2s of the minute boundary. FULL run (428 trades, +$480.35 at zero latency): losing only the races whose deadline is <=2s gives -$154; <=3s -$297; <=5s -$460. TEST goes -57 -> -214 at 3s. The live engine (paper.py:336-354) is a 2s poll loop over Swissquote (itself polled REST) plu
  - _Fix:_ Report the latency-stressed table as the headline and gate any go-live on the 2-5s row (currently deeply negative: do not ship this as a taker strategy). If the desk wants this signal, redesign around resting/maker entries where the race inverts — and then model the adverse selection of resting quot
- **[critical]** (✓ verified) Adverse selection: certain fills are toxic (hit 0.36, -17c/contract); the profitable fills are exactly the uncertain ones — and the desk's gap-vs-fill experiment has no power to detect this
  - The experiment as the desk framed it (fill-confirmed vs gapped-away outcomes) cannot run on this data: gaps are 12 of 1,452 intents (0.8%), so the comparison is statistically empty — itself evidence the filter models nothing. The honest version conditions on whether the touch survived the fill minute (side-price high vs limit, from candle OHLC): across all intents, fills whose level was never exceeded during the minute ('uncontested', guaranteed fillable) win 37.1% with -$0.174/contract; fills whose level was consumed ('contested') win 64.5% with +$0.055/contract at the pre-race price. On the 
  - _Fix:_ Add the contested/uncontested split to _metrics as a standard report; treat uncontested-only P&L (currently -$633 on FULL) as the certain-fill floor and any claimed edge above it as race-contingent. Any live gate must show shadow fills earning positive P&L on the uncontested subset alone.
- **[major]** (✓ verified) No depth constraint anywhere in the backtest; actual order sizes (median 55, max 120 — not the quoted 10-60) exceed printed liquidity at the level half the time and live touch depth is frequently thinner
  - backtest.py:288-311 fills the full intent.count unconditionally — the only liquidity gate is min_recent_volume=100 contracts/3min (strategy.py:56-58), which is volume, not depth, and whose comment ('Live equivalent: size at the touch >= our count') is asserted, never enforced in the backtest. Measured: chosen-config trade counts are 28-120, median 55 (the desk's '10-60 contracts' description understates its own sizing). Prod trades tape, FULL run's 340 contested fill minutes: in 50.6% the TOTAL volume printed at/inside the limit before the level left is smaller than the desk's own count (ratio
  - _Fix:_ Cap fill count at tape-verified size: fill_count = min(count, volume printed at/inside limit before the first beyond-limit print), and add partial-fill accounting; in live code enforce the size-at-touch>=count rule the comment promises (paper.py does check it; the backtest that produced the numbers 
- **[major]** (✓ verified) What the backtest can and cannot claim about the live engine (signal provenance and decision cadence)
  - CAN claim: the minute-boundary divergence between the re-anchored GC=F/SI=F/HG=F bar close and the contract quote has genuine predictive content at 60s horizon — filling at the NEXT candle's side CLOSE (60s of latency, no limit protection) still averages +2.2c/contract gross across all intents (vs +3.3c at open). Yahoo's ~10-min real-time delay does not corrupt the backtest itself (historical bars are final, and load_underlying's bar-close-at-T mapping in backtest.py:108-116 is causal). CANNOT claim: (a) that any of that edge is capturable — the tape shows capture requires <2-5s reaction (see 
  - _Fix:_ State these bounds in docs/KALSHI.md §4 explicitly; disable live entries in sessions with no backtest coverage (weekend/holiday windows); before scaling, record live Swissquote-vs-(re-anchored futures) signal deltas at decision instants to bound trigger disagreement.
- **[major]** (✓ verified) The shadow adapter — the designated go-live gate — has the same zero-latency fill flaw the backtest does
  - docs/KALSHI.md §4 gates go-live on 'live SHADOW results (prod-book fills, recorded live)'. But paper.py:237-255 books a shadow fill at intent.limit_price whenever the SAME orderbook snapshot used to make the decision shows touch size >= count — one REST GET serves as both signal and execution, so shadow mode also awards fills at the pre-race price with zero decision-to-execution latency and will therefore reproduce, not test, the backtest's optimism. Given the measured 1.4-4.1s median race deadlines, the shadow record will systematically overstate live economics in exactly the contested subset
  - _Fix:_ Make shadow two-phase: on intent, wait a drawn latency (e.g. 0.5-2s jitter plus measured REST RTT), re-fetch the book, and fill only if the limit is still at/inside the touch with size >= count; log both phases so the live race-loss rate is measured rather than assumed.
- **[note]** (✓ verified) Tick-granularity concern (2dp candles vs deci-cent wings) checked and NOT a material defect for the chosen config

## Lookahead / leakage

- **[major]** (✓ verified) Fill model is a zero-latency fill at the decision-instant quote; the 'next-candle-open' no-lookahead gate is empirically inoperative and all measured edge sits inside the unmodeled sub-minute race
  - backtest.py:288-306 claims conservatism: send a limit at the decision touch, fill only if the NEXT candle's side-price open is at/inside the limit ('book gapped away -> no fill'). Measured on the harvested candles (/tmp/claude-0/-home-user-quantfirm/90d61158-fb54-5b42-ac82-1d5fa3b34cdc/scratchpad/review/lookahead_exp1.py E4): Kalshi bid/ask candle opens are carry-forward states, ask_open(T+60)==ask_close(T) in 98.72% (gold), 98.44% (silver), 96.04% (copper) of adjacent minutes, and the gate rejected only 3 of 215 TEST intents (final_eval.json skipped.gapped_away=3). So the engine effectively a
  - _Fix:_ In /home/user/quantfirm/quantfirm/kalshi/backtest.py, stop treating next-candle-open as an adverse-move test (it is carry-forward): add a fill mode that (a) rejects or re-prices fills when the side-price HIGH of the fill candle exceeds the limit (probability-of-race model), and (b) reports the moved
- **[minor]** (✓ verified) Train/test and walk-forward windows both include the boundary instant: markets closing exactly on a split/fold boundary are counted in BOTH windows
- **[note]** (✓ verified) Chosen test config was not selected by the repo's codified sweep rule; its actual criterion is uniquely satisfied by exactly 1 of 192 configs (forking-paths flag), and 'evaluated ONCE on test' was actually two test runs
- **[note]** (✓ verified) Adversarial verification result: the timestamp-alignment, candle-semantics, vol-ordering, anchor, and diurnal-learning surfaces survive attack — no hard lookahead found

## Statistics / methodology

- **[critical]** (✓ verified) The +120.9% train result is below the expected MAXIMUM of pure noise for 192 trials; DSR ~= 0.27 vs the firm's 0.95 gate
  - Applying /home/user/quantfirm/docs/FIRM.md Section 2 to this evaluation (recomputed from /tmp/claude-0/-home-user-quantfirm/90d61158-fb54-5b42-ac82-1d5fa3b34cdc/scratchpad/results/sweep_train.json and reruns of quantfirm/kalshi/backtest.py): the 192 train-sweep trials have cross-trial annualized-Sharpe sd = 2.96 over T=14 daily observations. FIRM's own SR0 formula gives an expected max noise Sharpe SR0(N=192) = 8.14 annualized. The chosen config's train Sharpe is 5.65 — BELOW the noise maximum — so DSR = 0.27 (skew 0.50, kurt 2.50 corrected), and since SR_hat < SR0 no amount of additional days
  - _Fix:_ Never cite +120.9%/Sharpe 5.65 without the N=192 discount. Report DSR (0.27, or 0.70 at maximal dedup) and state explicitly that the train result is consistent with zero edge. Register all 1,344+ trials in any future DSR computation.
- **[critical]** (✓ verified) The primary evaluation (3-fold walk-forward) shows NEGATIVE OOS P&L and is powerless to detect any plausible edge; 3 folds establish nothing
  - Reran the three OOS folds of /tmp/claude-0/.../scratchpad/results/walkforward.py exactly (matches walkforward.json): W35 -129.82 (n=137, hit 0.409), W36 -215.71 (n=83), W37 +95.09 (n=44); total -$250.44 over 264 trades. Measured per-trade pnl sd = $21.96 (much larger than the $5-15 assumed in the desk's back-of-envelope; quarter-Kelly stakes of ~$25 on binaries produce +-$25-60 swings). Pooled per-trade t = -0.70; fold-level mean -$83.5, sd $160.5, t = -0.90; per-contract (sizing-independent) mean -0.3c, t = -0.11. The 95% CI on the 3-week OOS total is -250 +- 699, i.e. [-$949, +$449]. Power a
  - _Fix:_ State the walk-forward result as: 'OOS total -$250 over 3 folds; t ~= -0.7 to -0.9; the procedure demonstrates no edge and cannot demonstrate any edge smaller than ~$185/week at this fold count.' Any go/no-go gate must be sized from the measured per-trade sd ($22) and fold sd ($160), not hoped-for s
- **[critical]** (✓ verified) The pre-registered strategy is refuted by its own data: default params lose ~50% on BOTH splits; every positive number comes from gates fitted to 14 days in-sample
  - Ran the registered defaults of /home/user/quantfirm/quantfirm/kalshi/strategy.py (Params(): theta=0.05, price 0.08-0.92, tau 120-780) through the backtest: TRAIN -49.6% (hit 0.391, Sharpe -3.59), TEST -53.5% (hit 0.395), FULL -57.8%. The 'theory-motivated' gates (no longshots, mid-window tau, theta=7c) that turn -$248 train into +$604 train (a +$852 in-sample swing) were derived from an autopsy of that same 14-day train split — docs/KALSHI.md Section 4 admits this — and the sweep grid itself encodes the autopsy (3 of 4 price bands exclude longshots, 2 of 4 tau bands are the autopsy's mid-windo
  - _Fix:_ Re-label the gates as exploratory loss-avoidance filters derived in-sample. The only claim they support is 'the ungated version is worse'; they carry zero evidence of positive edge. Any future pre-registration must freeze gates BEFORE the sweep that evaluates them.
- **[major]** (✓ verified) All three FIRM.md Section-2 anti-overfitting gates fail: PBO = 0.40 (gate <= 0.10), walk-forward efficiency = -0.31 (gate >= 0.5), OOS consistency 1/3 folds positive — Score = 0
  - Recomputed with all 192 sweep configs rerun on both splits (daily P&L collected; script output in /tmp/claude-0/.../scratchpad/results/_adv_grid_both.json): (a) CSCV PBO with S=6 contiguous day-blocks over the 23 trading days: the IS-best config lands below the OOS median in 40% of splits -> PBO = 0.40 vs gate 0.10. (b) Walk-forward efficiency (median OOS per-week pnl / median selection-window per-week pnl) = -129.8 / 414.7 = -0.31 vs gate >= +0.5. (c) Positive OOS in 1 of 3 folds vs FIRM's '>= 5 of 8 folds' consistency requirement. (d) Test max drawdown 58.6% (66.1% under 1c slippage stress) 
  - _Fix:_ Apply the firm's own eval verbatim and report Score = 0 in the PR. Do not substitute a weaker bespoke evaluation (3-fold WF with a max-pnl selection statistic) for the registered one and describe the desk as validated.
- **[major]** (✓ verified) The walk-forward is contaminated: it was designed AFTER all of its OOS data had been examined, its grid encodes the autopsy, and its only positive fold is a truncated 3-day week
  - Timeline reconstructed from the artifacts: the 192-sweep (sweep_train.json) exposed per-week P&L for W33-W35a; the single test look (final_eval.json) exposed per-week and per-metal P&L for W35b-W37. The walk-forward (walkforward.py, built afterwards) uses W35, W36, W37 as its OOS weeks — 100% of its 'out-of-sample' days had already been inspected, and fold 1's OOS week even straddles the original train/test split (Aug 24-26 was inside the sweep's training data). The 384-config WF grid adds the flow/stale regime gate and keeps the autopsy-derived tau/price bands; the selection rule (n>=25, posi
  - _Fix:_ Present the walk-forward as an in-hindsight robustness illustration, not primary OOS evidence. A clean evaluation now requires data that did not exist when the code was written: live-forward weeks (shadow mode) with the procedure and selection rule frozen in the repo before each week begins.
- **[major]** (✓ verified) Sample-coverage defects: effective train window is 13.5 days (not 4 weeks), and copper was traded in test with ZERO train history
  - Verified from the harvest: underlying 1-min bars (yf_*_1m.csv) span only 2026-08-13 16:17Z -> 2026-09-10 16:06Z (yfinance 30-day 1m limit), while the markets files span Jul 31 -> Sep 10; the backtest silently skips decisions without underlying (skipped.no_underlying = 17,317 in train, 5,701 in test — dwarfing the ~215 trades taken). So 'train' is effectively Aug 13-27 (~13.5 days) and W33's 'week' in all selection windows is ~1.5 days of data. Worse: KXCOPPER15M's first settled market closes 2026-08-27 21:00Z — entirely inside the test split — so the test evaluated copper (77 trades, -$91.87 r
  - _Fix:_ Report the effective coverage window (Aug 13-Sep 10, ~4 weeks total) everywhere the sample is described; exclude copper from any split whose train side has no copper data (mirroring the live engine, which already disables copper); never cite per-metal P&L signs from <3 weeks of data.
- **[minor]** (✓ verified) Metric hygiene inflates headline numbers: returns on fixed $500 base under compounding sizing, sqrt(252) annualization on a ~5.5-day market, Sharpe from 14 daily observations
- **[note]** (✓ verified) Recommended honest claim set for the PR/docs

## Venue mechanics

- **[major]** (✓ verified) Kelly sizing omits the fee from all-in cost, violating the desk's own pre-registered spec
  - fair.py:17 and the research SYNTHESIS both register sizing as f* = (q-a)/(1-a) with a = price + fee/contract, but strategy.py:141 calls kelly_fraction(q, cost) where cost is price(+slippage) only — the fee is subtracted from edge (line 130/135) but never enters the Kelly denominator/numerator. Recomputed: at q=0.56, price=0.50, fee=2c/ct the coded f*=0.1200 vs spec 0.0833 (+44%); at q=0.60/0.50 +20%. The 5% stake cap absorbs it only for fat edges: on the chosen test config (theta=0.07, hl=30, tau 120-600, price 0.35-0.92) 62/215 trades (29%) are sized by the Kelly leg, not the cap. Patched rer
  - _Fix:_ In strategy.decide(), size with all-in cost: f = p.kelly_mult * kelly_fraction(q, cost + taker_fee(1, cost)) (per-contract fee share is the conservative choice), at /home/user/quantfirm/quantfirm/kalshi/strategy.py:141.
- **[major]** (✓ verified) The -10%/day loss stop does not survive restarts of the live paper engine
  - paper.py:184-195 (_entries_allowed) keeps the day baseline in self._day, which is in-memory only; PaperState (paper.py:58-81) never persists it. The engine is explicitly single-shot ('run N minutes, persist, exit — safe to rerun', paper.py:20-21), so under the documented operating mode every restart re-baselines day-start equity at CURRENT (post-loss) equity and the stop can never trip cumulatively across runs. Verified by execution: seeded a state file with cash 400 (=-20% on the day) into a fresh PaperEngine — _entries_allowed() returned {'shadow': True, 'maker': True}. The backtest (backtes
  - _Fix:_ Persist {date, day_start_equity per book, realized_today} in the PaperState JSON and restore it in _entries_allowed; reset only on UTC date change. /home/user/quantfirm/quantfirm/kalshi/paper.py:184-195.
- **[minor]** (✓ verified) Edge gate charges a different fee than the venue: per-contract cent-ceil in decide() vs per-order fee at execution
- **[note]** (✓ verified) docs/KALSHI.md settlement-rounding claim is wrong (2dp) — actual precision is per-metal (gold 2dp, silver 3dp, copper 5dp); tie rule itself verified immaterial and correctly handled
- **[note]** (✓ verified) Attack surface that survived: fees, NO-side arithmetic, bankroll conservation, Kelly closed form, orderbook/order-path semantics all verified against data, live venue, and the venue OpenAPI spec

## Live / backtest parity

- **[critical]** (✓ verified) Live vol clock is a different estimator than the backtest's; sigma error alone manufactures/destroys entry edges
  - paper.py:144-164 (warm_vol_from_prints) seeds the live EWMA from 15-min settlement prints, feeding each window's lr15/sqrt(15) as 15 IDENTICAL pseudo-1-min returns. The EWMA halflife is 30 updates, so after warmup sigma is determined by the last ~2-4 fifteen-minute prints (~2-4 effective degrees of freedom) — versus the backtest clock (backtest.py:157-163) which is an EWMA over ~43 effective 1-min futures bars. VERIFIED by running: (a) exact reproduction — replaying warm_vol_from_prints on the actual API prints available at session start 16:35:48Z reproduces the session-logged sigmas to 3 sig 
  - _Fix:_ Warm the live EWMA from the same object the backtest uses: fetch recent 1-min underlying history (or persist VolEstimator state across sessions and gate entries until >=N live 1-min Swissquote updates, e.g. one halflife = 30 updates). At minimum, skip warm pairs whose close-to-close dt != 900s, and 
- **[major]** (✓ verified) The live session is running the DEFAULT gates, not the documented pre-registered config — trades their own autopsy refuted
  - research/kalshi_backtest.md declares the live taker shadow experiment as theta=0.07, tau in [300,600], price in [0.35,0.92] (chosen because the train autopsy showed first-3-minutes entries and longshot buys <=0.30 are 'reliably negative'). VERIFIED from state/kalshi_paper_decisions.jsonl and state/kalshi_paper_trades.csv that the actual session (started 2026-09-10T16:35:48Z, still running during this audit) runs Params() defaults: the settled silver fill was 58 YES @ 0.19 (price 0.19 < 0.35), at tau_s=120 (< 300), with edge +0.0553 (< 0.07) — impossible under the documented config on three ind
  - _Fix:_ Wire the chosen config as cmd_paper defaults (or require an explicit --config), and write the full effective Params dict into the state file and each decisions-log header so every session is auditable against the registered experiment. File: /home/user/quantfirm/quantfirm/kalshi/cli.py lines 93-107,
- **[major]** (✓ verified) Daily loss stop re-arms from depleted equity on every restart — unbounded daily loss for a deliberately single-shot engine
  - paper.py:183-195 (_entries_allowed) keeps the day-start equity in self._day, an in-memory dict that is never persisted to the state file. The engine is explicitly designed to be rerun in short sessions (docstring line 20: 'run N minutes, persist, exit — safe to rerun'; CLI default 60 minutes). VERIFIED by running: engine at cash 500 sets day start 500; after -9.8% (cash 451) entries still allowed; a restarted engine the SAME UTC day re-baselines day start to 451 and allows losses down to 405.9; at cash 406 (-18.8% on the day) entries remain allowed, where the backtest (backtest.py:227-233) hal
  - _Fix:_ Persist {date, start-equity per book} into the state JSON on save and reload it in PaperState.__init__; only re-baseline when the UTC date actually changes. File: /home/user/quantfirm/quantfirm/kalshi/paper.py lines 141, 183-195, 75-81.
- **[major]** (✓ verified) Cross-adapter dedupe: maker (and demo) fills silence the shadow taker record mid-window — happened in 4 windows of today's session
  - paper.py:329 gates each tick with `any(p.ticker == tkr for p in self.state.open)` across ALL adapters, then skips decide() entirely. So a fill in the zero-fee maker book (or a demo-env fill against the fake demo book) blocks the ECONOMIC shadow record for the remainder of that window. VERIFIED from the live decisions log: KXGOLD15M-26SEP101315-15 decisions stop at 17:04:02 (tau=658s — the maker fill moment), silencing the taker path for ~11 minutes of a 15-minute window; KXSILVER15M-26SEP101315-15 stops at 17:07:14 (tau=466); both -1330-30 tickers stop at 17:24:04/06 (tau~355, matching the two
  - _Fix:_ Track dedupe per adapter: the shadow entry gate checks only adapter=='shadow' positions; demo mirrors shadow intents but its fills must never feed the entry gate; add a per-ticker intent cooldown (e.g. 30-60s) for the demo resend path. File: /home/user/quantfirm/quantfirm/kalshi/paper.py lines 329, 
- **[major]** (unverified) Live shadow fill model is strictly MORE optimistic than the backtest's — the record meant to de-bias the backtest is biased the other way
  - Backtest (backtest.py:288-311): an intent becomes a fill only if the NEXT minute candle's side-price OPEN is still at/inside the limit — the book must not gap away for up to 60s; deliberately conservative. Live shadow (paper.py:378-396): the intent fills INSTANTLY at the just-fetched touch whenever displayed top-of-book size >= count — zero latency, no persistence requirement, no confirmation. In the exact scenario the strategy monetizes (spot jumped, quote stale), displayed size at the stale touch is what vanishes first to faster takers; live shadow assumes we always beat them. Additionally (
  - _Fix:_ Make shadow fills two-phase: record the intent, re-fetch the orderbook after a configured latency (e.g. 500ms-2s), fill only if the limit is still marketable and size still covers; log both optimistic and confirmed fills so the gap is measured rather than assumed. File: /home/user/quantfirm/quantfir
- **[minor]** (✓ verified) Live 1-min vol update has no gap guard: a Swissquote outage injects a compressed multi-minute return the backtest would drop
- **[minor]** (✓ verified) Maker fill detection keys on the deprecated `taker_side` trade field — silent maker-book death when Kalshi drops it (removable since 2026-05-14)
- **[minor]** (✓ verified) Demo adapter: fractional IOC fills booked as int(filled) while cash is debited for the fractional amount; otherwise the V2 parse is correct
- **[minor]** (unverified) Settlement and crash-recovery edges: voided markets pin cash forever; settle rows can double-log; demo positions can duplicate after a crash
- **[note]** (✓ verified) Audit items that held up, and residual small parity deviations

## Disposition

All verified critical/major findings were either fixed in code (fill model,
Kelly fee, daily-stop persistence, per-adapter dedupe, two-phase shadow fill,
vol-clock parity, config wiring, window boundary) or folded into the honest
verdict (taker edge is negative at realistic latency; Score = 0 under
FIRM.md §2). Minor/unverified items are tracked in the code comments at the
relevant sites. The maker leg is the only surviving candidate and is a live,
not backtested, experiment.
